# nixpacks.toml (VERSIÓN FINAL Y ROBUSTA)

# 1. Fase de proveedores: Instalar Python, Redis y las librerías necesarias.
[phases.setup]
nixPkgs = ["python3", "gcc", "libjpeg", "zlib", "redis"]

# 2. Fase de inicio: Aquí está el cambio fundamental.
[start]
# Usamos un script de inicio para asegurar que todo se configure correctamente.
# El comando ahora es mucho más limpio.
cmd = "./start.sh"